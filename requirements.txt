# numpy reference path (what produced the current figures)
numpy>=1.26
matplotlib>=3.8

# PennyLane path (src/pennylane_pipeline.py, scripts/run_pennylane.py).
# The Scientific Track environment pins this version:
#   uv sync --project "Scientific Track"
pennylane==0.44.1

# N >= 10 sparse solver and PDF generation
scipy>=1.11
reportlab>=4.0
pillow>=10
